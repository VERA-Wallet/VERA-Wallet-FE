/**
 * 테스트가 못 박는 과세연도.
 *
 * 데모 픽스처의 시각은 시계를 따라 움직인다(`lib/mock/demo-calendar.ts`).
 * 그래야 해가 바뀌어도 화면이 "계산할 거래 없음"으로 죽지 않는다.
 * 반면 락인된 기대값(1,015.44 등)은 움직이면 안 된다 —
 * 프랑스 PFU·이탈리아 대체세처럼 **연도에 따라 세율이 바뀌는** 룰셋이 있어서,
 * 픽스처 연도와 기대값이 따로 흐르면 어느 날 갑자기 이유 없이 빨개진다.
 *
 * 픽스처 생성과 `taxYear` 인자에 **항상 이 값을 함께** 넘긴다.
 */
export const FIXTURE_TAX_YEAR = 2025;
