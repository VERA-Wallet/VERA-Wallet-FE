import { ReportSettings } from "@/components/report/report-settings";

/** 계산 설정 — 계산 조건·추가 입력·연말 시가·계산 설정 요약. 가드는 상위 layout이 한다. */
export default function ExportSettingsPage() {
  return <ReportSettings />;
}
