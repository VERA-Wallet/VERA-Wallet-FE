import { ReportBasis } from "@/components/report/report-basis";

/** 계산 근거 — 판정 그룹·계산 내역·근거와 가정·확정 상태·앵커링 증명. 가드는 상위 layout이 한다. */
export default function ExportBasisPage() {
  return <ReportBasis />;
}
