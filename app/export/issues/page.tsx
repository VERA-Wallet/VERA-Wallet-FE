import { ReportIssues } from "@/components/report/report-issues";

/** 확인할 것 — 흔들리는 지점·판단 필요 항목·확인 필요 넛지. 가드는 상위 layout이 한다. */
export default function ExportIssuesPage() {
  return <ReportIssues />;
}
